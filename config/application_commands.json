{
    "global": [
        {
            "name": "checkem",
            "description": "Posts the ID of your message and checks for dubs or better."
        },
        {
            "name": "left-pad",
            "description": "Left-pads a message",
            "options": [
                {
                    "name": "message",
                    "description": "The message to pad.",
                    "type": 3,
                    "required": true
                },
                {
                    "name": "length",
                    "description": "The length to pad to.",
                    "type": 4,
                    "required": true
                },
                {
                    "name": "character",
                    "description": "The character to pad with.",
                    "type": 3,
                    "required": true
                }
            ]
        },
        {
            "name": "mdn",
            "description": "Searches MDN for the given query, returning the link for the first matched article.",
            "options": [
                {
                    "name": "query",
                    "description": "The query to search for.",
                    "type": 3,
                    "required": true
                }
            ]
        },
        {
            "name": "shuffle",
            "description": "Shuffles the provided message, word by word.",
            "options": [
                {
                    "name": "message",
                    "description": "The message to shuffle.",
                    "type": 3,
                    "required": true
                }
            ]
        },
        {
            "name": "test",
            "description": "Test command."
        },
        {
            "name": "version",
            "description": "Print version information."
        },
        {
            "name": "year-progress",
            "description": "Prints a progress bar that shows how far through the year we are."
        }
    ]
}
